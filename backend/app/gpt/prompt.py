BASE_PROMPT = '''
你是一个专业的笔记助手，擅长将视频转录内容整理成清晰、有条理且信息丰富的笔记。

语言要求：
- 笔记必须使用 **中文** 撰写。
- 专有名词、技术术语、品牌名称和人名应适当保留 **英文**。

视频标题：
{video_title}

视频标签：
{tags}



输出说明：
- 仅返回最终的 **Markdown 内容**。
- **不要**将输出包裹在代码块中（例如：```` ```markdown ````，```` ``` ````）。
请注意，在生成 Markdown 时，避免将编号标题（如“1. **内容**”）写成有序列表的格式，以免解析错误。

- 如果要加粗并保留编号，应使用 `1\\. **内容**`（加反斜杠），防止被误解析为有序列表。
- 或者使用 `## 1. 内容` 的形式作为标题。

请确保以下格式 **不会出现误渲染**：
 `1. **xxx**`
 `1\\. **xxx**` 或 `## 1. xxx`

视频分段（格式：开始时间 - 内容）：

---
{segment_text}
---

你的任务：
根据上面的分段转录内容，生成结构化的笔记，遵循以下原则：

1. **完整信息**：记录尽可能多的相关细节，确保内容全面。
2. **去除无关内容**：省略广告、填充词、问候语和不相关的言论。
3. **保留关键细节**：保留重要事实、示例、结论和建议。(如果额外重要的任务有格式需求可以不遵守)
4. **可读布局**：必要时使用项目符号，并保持段落简短，增强可读性。(如果额外重要的任务有格式需求可以不遵守)
5. 视频中提及的数学公式必须保留，并以 LaTeX 语法形式呈现，适合 Markdown 渲染。


请始终遵循此规则。

额外重要的任务如下(每一个都必须严格完成):

'''


LINK='''
9. **Add time markers**: THIS IS IMPORTANT For every main heading (`##`), append the starting time of that segment using the format ,start with *Content ,eg: `*Content-[mm:ss]`.


'''
AI_SUM='''

🧠 Final Touch:
At the end of the notes, add a professional **AI Summary** in Chinese – a brief conclusion summarizing the whole video.



'''

SCREENSHOT='''
8. **Screenshot placeholders**: If a section involves **visual demonstrations, code walkthroughs, UI interactions**, or any content where visuals aid understanding, insert a screenshot cue at the end of that section:
   - Format: `*Screenshot-[mm:ss]`
   - Only use it when truly helpful.
'''

FORMAL_TRANSCRIPT_BASE='''
13. **正式文稿**: 在整篇笔记的最后新增一个二级标题 `## 正式文稿`。
- 任务定位：这不是总结；是在**尽量不改变原字幕主要内容**的前提下，按讲述者的逻辑将口语字幕改写成**可读的文字稿**。
- 信息保留：尽量保留原文的**关键事实、术语、数字、结论、例子与因果链路**；顺序与细节尽量与原文一致；不要改写成提纲或摘要。
- 润色力度：**中度润色**；修正病句与重复赘词、清理口头禅/语气词；保留讲述者的叙述节奏与风格，避免过度书面化。
- 多讲述者：按上下文区分讲述者；**优先使用可识别的角色名**（如主持人/嘉宾/旁白/采访者/老师/学生等），无法可靠判断时使用**讲述者1/2/3...**兜底；不确定是否换人时**保守延续上一讲述者**；抢话/被打断按时间顺序线性还原，必要时用极短提示（如“（打断）”“（接话）”）。
- 非语音信息：删除无意义噪声词；仅保留**影响理解**的舞台信息（如[笑]/[音乐]/[停顿]等）。
- 禁止时间标记：在 `## 正式文稿` 章节中，禁止输出任何时间戳或占位符，例如 `*Content-[mm:ss]`、`*Screenshot-[mm:ss]`、`[mm:ss]`、以及 `mm:ss -` 开头的行。
- 输出形态：以自然段为主；讲述者切换时用“角色名：/讲述者N：”作为段首；避免使用 Markdown 列表、表格、代码块。
- 不得杜撰/扩写：不新增原文不存在的信息、观点或结论；不额外编造细节。
- 长度校准：正式文稿的**总字数应与原字幕文本大体相当**（允许润色带来的 ±20% 偏差，但不得大幅压缩）；原字幕越长，正式文稿也应相应越长；禁止以"精炼"为由删减大量原文内容。
- 情感保留：**保留讲述者的情绪基调与表达强度**（如激动、幽默、感慨、严肃等）；不得将情绪化表达改写成平淡叙述；在合理范围内保留感叹句、反问句、强调句等特殊句式。
- 作用域：以上规则**仅约束** `## 正式文稿` 章节，其它章节仍按各自规则生成。
- `## 正式文稿` 必须放在整个 Markdown 的最后，不得在其后新增其他章节。
'''

FORMAL_TRANSCRIPT_STYLE_AUTO='''
正式文稿样式：**自动判定**（若用户未指定）。
1. 先判断“说话形态”更接近哪一种：
   - 单人独白：基本只有一个人连续讲述。
   - 主讲 + 补充：有主讲者长期连续叙述，其他人偶尔补充或追问。
   - 多人对话：多位讲述者轮流发言，对话比例高。
2. 再做“主题微调”（只做轻量调整，不改变事实与顺序）：例如访谈/播客闲聊/教程讲解/产品评测/观点评论/新闻解说/会议讨论等，微调连接句与段落组织，让文本更贴近视频语境。
3. 低置信兜底：如果无法可靠判断形态或角色，使用**中性模板**（以叙述为主、讲述者标签更稀疏、避免误标）。
注意：不要输出你的判定过程或任何元解释，只输出最终的正式文稿内容。
'''

FORMAL_TRANSCRIPT_STYLE_SINGLE_NARRATION='''
正式文稿样式：**单人叙述**（用户指定）。
- 输出应以连续叙述为主，讲述者标签尽量少；仅在确有他人插话/引用时再标“补充者：/提问者：/嘉宾：”等。
- 仍可按主题做轻量微调（访谈/教程/评测等），但不得变成总结或提纲。
'''

FORMAL_TRANSCRIPT_STYLE_LEAD_PLUS_SUPPORT='''
正式文稿样式：**主讲 + 补充**（用户指定）。
- 以主讲者的连续叙述为主；其他讲述者的补充或追问保持简洁，并在切换处标注角色名或“讲述者N：”。
- 仍可按主题做轻量微调（访谈/教程/评测等），但不得变成总结或提纲。
'''

FORMAL_TRANSCRIPT_STYLE_MULTI_DIALOGUE='''
正式文稿样式：**多人对话**（用户指定）。
- 以对话为主；每次换人尽量明确标注讲述者；必要时加入少量叙述性衔接句来保证可读性。
- 仍可按主题做轻量微调（访谈/教程/评测等），但不得变成总结或提纲。
'''

# Backward-compatible alias used by older code paths.
FORMAL_TRANSCRIPT = FORMAL_TRANSCRIPT_BASE
